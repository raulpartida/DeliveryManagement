<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rulop_000.deliverym">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/man"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Vista.EntregasActivity"
            android:label="@string/title_activity_entregas"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Vista.MapsActivity"
            android:label="@string/title_activity_maps"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Vista.DireccionesActivity"
            android:label="@string/title_activity_direcciones"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Vista.MercanciaActivity"
            android:label="@string/title_activity_mercancia"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Vista.PerfilActivity"
            android:label="@string/title_activity_perfil"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Vista.LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Vista.MenuActivity"
            android:label="@string/title_activity_menu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Vista.ConfiguracionActivity"
            android:label="@string/title_activity_configuracion"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Vista.AddClienteActivity"
            android:label="@string/title_activity_add_cliente"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Vista.SetConfiguracionActivity"
            android:label="@string/title_activity_set_configuracion"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Vista.CambioPalabraActivity" />
        <activity android:name=".Vista.EstatusSeleccionActivity" />
        <activity android:name=".Vista.OlvidoContrasenaActivity"></activity>
    </application>

</manifest>